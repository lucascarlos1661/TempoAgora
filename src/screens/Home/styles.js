import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        padding: 20
    },
    lastUpdate: {
        marginTop: 20
    },
    viewItems: {
        alignItems: 'center',
        marginVertical: 20
    },
    addNewCity: {
        alignItems:"center",
        marginVertical: 20
    }
})